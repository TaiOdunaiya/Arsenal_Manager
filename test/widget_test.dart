import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Arsenal Manager smoke test', (WidgetTester tester) async {
    // Placeholder — real integration tests require a running API server.
    expect(true, isTrue);
  });
}
